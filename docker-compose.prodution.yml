version: "3.9"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8002:8000
    depends_on:
      - db
    environment:
      - APP_ENV=production
    restart: always
    secrets:
      - secret_key
    networks:
      - app-netwroks
